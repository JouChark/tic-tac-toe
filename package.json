{
  "name": "tic-tac-toe",
  "version": "1.0.0",
  "description": "A tic tac toe game build with React, Express and Socket.io",
  "main": "app.js",
  "engines": {
    "node": "16.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "npm install --prefix ./client && npm run build --prefix ./client/",
    "dev": "nodemon app.js",
    "start": "node app.js"
  },
  "keywords": [],
  "author": "Uaura Menezes",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^3.0.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
